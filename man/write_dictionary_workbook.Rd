% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_dictionary_workbook.R
\name{write_dictionary_workbook}
\alias{write_dictionary_workbook}
\title{Write the dictionary workbook to disk}
\usage{
write_dictionary_workbook(wide_map, factor_map, match_by, save_path)
}
\arguments{
\item{wide_map}{The wide variable map built by \code{dict_init()}.}

\item{factor_map}{The candidate factor rows built by \code{mine_factors()}
(possibly zero rows).}

\item{match_by}{Character string. The variable-alignment strategy used by
\code{dict_init()} (\code{"position"} or \code{"name"}), used for the
Instructions text and the initial \code{Change_Log} entry.}

\item{save_path}{Character string. File path for the output \code{.xlsx}
dictionary.}
}
\value{
Invisibly returns \code{TRUE} on success. The primary output is the
\code{.xlsx} file written to \code{save_path}.
}
\description{
Internal helper for \code{dict_init()}. Builds the full
workbook, the Instructions sheet, the styled \code{Variable_Map_Wide} sheet,
the \code{Factor_Levels} sheet (with embedded \code{XLOOKUP} formulas
resolving \code{target_name}), the \code{Change_Log} sheet, and the
\code{Original_Metadata} snapshot, then saves it to \code{save_path}.
}
\keyword{internal}
