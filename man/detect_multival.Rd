% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_multival.R
\name{detect_multival}
\alias{detect_multival}
\title{Detect columns containing multi-value cells}
\usage{
detect_multival(data, sep = "[,;]", trim_ws = TRUE)
}
\arguments{
\item{data}{A data.frame or tibble to scan.}

\item{sep}{A single regex string used to split cells. Defaults to
\code{"[,;]"} (comma or semicolon).}

\item{trim_ws}{Logical. Whether to strip leading/trailing whitespace from
each extracted value before taking uniques. Defaults to \code{TRUE}.}
}
\value{
Invisibly returns a named list: one element per flagged column,
each a sorted character vector of its unique values. Empty list if none
are found.
}
\description{
Scans every character column in a data frame for cells that
split into more than one value under \code{sep}, indicating multiple
choices stored in a single cell. Prints a message naming any flagged
columns, and invisibly returns a named list of each flagged column's
unique atomic values (alphabetically sorted), ready to reuse as
\code{new_names} in \code{harm_split_multival()}.
}
\examples{
wave1 <- data.frame(
  id       = c("alice", "bob", "carol"),
  symptoms = c("headache; cramps", "None", "cramps; headache")
)
detect_multival(wave1)

}
\seealso{
\code{\link{split_multival}}
}
