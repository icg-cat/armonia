% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harm_add_timepoint.R
\name{harm_add_timepoint}
\alias{harm_add_timepoint}
\title{Append a New Longitudinal Timepoint}
\usage{
harm_add_timepoint(master_data, new_data, by_id, suffix, verbose = TRUE)
}
\arguments{
\item{master_data}{A data.frame/tibble representing the current master
dataset (Time 1).}

\item{new_data}{A data.frame/tibble representing the new timepoint (Time N+1).
Must be already processed by dict_apply().}

\item{by_id}{Character string. The name of the distinct variable to join by
(e.g., "participant_id"). Must exist in both datasets.}

\item{suffix}{Character string. The suffix to append to new variables
(e.g., "_w2", "_t2"). Must start with an underscore/separator to ensure
readability and comply with snake_case referencing across objects.}

\item{verbose}{Logical. If TRUE, prints a summary of the join statistics.}
}
\value{
A single wide-format tibble containing both datasets.
}
\description{
Safely joins a new wave of data to an existing master dataset.
It automatically handles variable renaming (suffixing) to prevent collisions
and uses a full join to preserve all participants (attrition + recruitment).
}
\examples{
master   <- data.frame(id = c("alice", "bob"), wellbeing = c(8, 5))
new_wave <- data.frame(id = c("alice", "bob"), wellbeing = c(9, 6))
harm_add_timepoint(master, new_wave, by_id = "id", suffix = "_w2")

}
