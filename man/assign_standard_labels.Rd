% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign_standard_labels.R
\name{assign_standard_labels}
\alias{assign_standard_labels}
\title{Assign standard labels to factor variables using the dictionary}
\usage{
assign_standard_labels(data, dict)
}
\arguments{
\item{data}{A data.frame whose factor columns carry integer-coded levels.
Typically the output of \code{harm_bind_waves()}.}

\item{dict}{Path to the dictionary containing the Factor_Levels sheet where standard_codes and standard_labels are specified.}
}
\value{
A data.frame identical to \code{data} with factor levels replaced
by their \code{standard_label} strings.
}
\description{
Iterates over all factor columns in \code{data} and replaces
their numeric levels with the corresponding \code{standard_label} values
from the dictionary. Intended to be called on the master dataset produced
by \code{harm_bind_waves()}, after factors have been codified as integers
by \code{dict_apply()} and after the dictionary has been validated by
\code{dict_validate()}.
}
\examples{
data <- data.frame(eye_color = factor(c("1", "2", "1")))
dict_path <- tempfile(fileext = ".xlsx")
openxlsx::write.xlsx(
  list(Factor_Levels = data.frame(
    target_name    = c("eye_color", "eye_color"),
    standard_code  = c("1", "2"),
    standard_label = c("Hazel", "Green")
  )),
  file = dict_path
)
assign_standard_labels(data, dict_path)

}
\seealso{
\code{\link{dict_validate}}
}
